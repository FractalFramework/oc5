{% extends ajaxMode == 'false' ? 'main.html.twig' : 'empty.html.twig' %}
{%  block main %}
    <h2>Inscription</h2>
        <div class="frame-green">
            {{welcome}}
        </div>
        <p>Bienvenue, {{name}}</p>
    
    <a id="logbt" class="btsav" onclick="bj(this)" data-bj="main|home">
            Retour à la Home</a>
{% endblock %}
