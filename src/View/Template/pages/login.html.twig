{% extends ajaxMode == 'false' ? 'main.html.twig' : 'empty.html.twig' %}
{%  block main %}
    <h2>Inscription</h2>
    
    {% if error %}
        <div class="alert alert-warning">{{error}}</div>
    {% endif %}

        <div class="alert alert-primary">
            <div class="">
                <input type="text" id="name" placeholder="Pseudo" value="{{name}}" required>
                <label for="name">Pseudo</label>
            </div>
            <div class="">
                <input type="password" id="pswd" placeholder="Mot de passe" required>
                <label for="pswd">Mot de passe</label>
            </div>
            <a class="btn btn-outline-primary" onclick="bj(this)" data-bj="main|logon||name,pswd">
                Login !</a>

            <a class="btn btn-outline-secondary" onclick="bg(this)" data-bj="register|register">
                S'enregistrer ?</a>
            <div id="register"></div>
        </div>
{% endblock %}
